{
  "name": "beautiful-help-document",
  "type": "module",
  "version": "0.0.3",
  "private": true,
  "scripts": {
    "dev": "pnpm -C playground dev & deno --allow-env --allow-net --env-file=.env.local -- proxy-server/index.deno.ts",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "clean": "rimraf **/dist **/node_modules",
    "bumpp": "bumpp -r"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^3.12.1",
    "bumpp": "^9.9.1",
    "eslint": "^9.17.0",
    "pnpm": "^9.15.4",
    "rimraf": "^6.0.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.5"
  }
}
